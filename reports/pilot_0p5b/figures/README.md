# Pilot figure outputs

`Matched 0.5B pilot - not the final benchmark`

This directory intentionally contains no result figure during CPU configuration freeze. Future figures must be generated only from finalized saved CSV/JSON artifacts, never from hand-entered values. Planned views are reward by seed/algorithm; format/valid-expression/number-usage; group variance/zero-advantage; loss/grad norm; token usage; and peak VRAM/wall time/GPU-hours/cost. Missing telemetry must be omitted or shown unavailable, never zero-filled.
