-r base.txt
# TRL 0.22+ includes the modern GRPO trainer generation and PyTorch 2.8-era support.
# PPO APIs evolve quickly; validate the adapter in the smoke test before the paid main run.
trl>=0.22,<1

